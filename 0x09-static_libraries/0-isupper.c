#include "main.h"
/**
 * _isupper -checks for uppercase character
 * @c: integer to be checked
 * Return: returns 1 or 0
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
return (0);
return (c >= 'A' && c <= 'Z');
}
