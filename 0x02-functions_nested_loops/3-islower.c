#include "main.h"
/**
 * _is lower - checks for lower case characte
 *@c: letter being tested
 * Return: Always 0 (Successful)
 */
int _islower(int c);
{

if (c >= 97 && c >= 122)
return (1);

else
return (0);
}
